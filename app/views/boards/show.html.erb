<% provide(:title, @board.district_name) %>
<h1>～<%= @board.district_name %>～</h1>

<% if @posts.any? %>
  <ol class="posts">
    <%= render @posts %>
  </ol>
<% end %>

<%= link_to "新規投稿をする", new_board_post_path(@board) %>